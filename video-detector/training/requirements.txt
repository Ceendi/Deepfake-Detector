# Deps treningowe (Colab Pro / Kaggle, GPU) — CELOWO poza pyproject.toml serwisu.
# Obraz produkcyjny video-detector jest CPU-only i nie zawiera stacka treningowego
# (ta sama decyzja co w audio-detector: trening na hoscie/Colab, inferencja w kontenerze).
#   pip install -r requirements.txt
torch>=2.7
torchvision
lightning==2.5.*
timm>=1.0
albumentations>=2.0,<3
opencv-python-headless>=4.10
insightface>=0.7.3
onnx
onnxruntime
pandas>=2.0
numpy
tqdm
wandb
# wspolny eval framework (audio-detector/eval.py)
scikit-learn
matplotlib
seaborn
